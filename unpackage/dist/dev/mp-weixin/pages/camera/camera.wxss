/**
 * 这里是uni-app内置的常用样式变量
 *
 * uni-app 官方扩展插件及插件市场（https://ext.dcloud.net.cn）上很多三方插件均使用了这些样式变量
 * 如果你是插件开发者，建议你使用scss预处理，并在插件代码中直接使用这些变量（无需 import 这个文件），方便用户通过搭积木的方式开发整体风格一致的App
 *
 */
/**
 * 如果你是App开发者（插件使用者），你可以通过修改这些变量来定制自己的插件主题，实现自定义主题功能
 *
 * 如果你的项目同样使用了scss预处理，你也可以直接在你的 scss 代码中使用如下变量，同时无需 import 这个文件
 */
/* 颜色变量 */
/* 行为相关颜色 */
/* 文字基本颜色 */
/* 背景颜色 */
/* 边框颜色 */
/* 尺寸变量 */
/* 文字尺寸 */
/* 图片尺寸 */
/* Border Radius */
/* 水平间距 */
/* 垂直间距 */
/* 透明度 */
/* 文章场景相关 */
.camera-page.data-v-7b8d50ad {
  position: fixed;
  top: 0;
  left: 0;
  width: 100%;
  height: 100vh;
  background-color: #000;
}
.camera.data-v-7b8d50ad {
  position: absolute;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  z-index: 1;
}
.pose-reference.data-v-7b8d50ad {
  position: absolute;
  top: 50%;
  left: 50%;
  transform: translate(-50%, -50%);
  width: 80%;
  height: 70%;
  z-index: 2;
}
.custom-navbar.data-v-7b8d50ad {
  position: absolute;
  top: var(--status-bar-height);
  left: 0;
  right: 0;
  height: 90rpx;
  /* 自定义导航栏高度 */
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 0 10rpx;
  z-index: 3;
}
.nav-left.data-v-7b8d50ad, .nav-right.data-v-7b8d50ad {
  width: 60rpx;
  height: 60rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  padding-top: 50rpx;
}
.nav-icon.data-v-7b8d50ad {
  width: 40rpx;
  height: 40rpx;
}

/* 固定在底部的操作栏 */
.fixed-action-bar.data-v-7b8d50ad {
  position: fixed;
  bottom: 0;
  left: 0;
  right: 0;
  z-index: 100;
  width: 100%;
  background: rgba(0, 0, 0, 0.8);
  -webkit-backdrop-filter: blur(20px);
          backdrop-filter: blur(20px);
  border-top: 1rpx solid rgba(255, 255, 255, 0.1);
  padding: 40rpx 0;
}

/* 操作项容器 */
.action-items.data-v-7b8d50ad {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 0 60rpx;
}

/* 每个操作项的通用样式 */
.action-item.data-v-7b8d50ad {
  display: flex;
  justify-content: center;
  align-items: center;
  height: 100rpx;
}

/* 相册预览 */
.album-preview.data-v-7b8d50ad {
  width: 80rpx;
  height: 80rpx;
  border-radius: 16rpx;
  overflow: hidden;
  border: 2rpx solid rgba(255, 255, 255, 0.3);
}
.album-preview .preview-image.data-v-7b8d50ad {
  width: 100%;
  height: 100%;
}

/* 拍照按钮容器 */
.shutter-container.data-v-7b8d50ad {
  flex: 1;
  max-width: 200rpx;
}

/* 拍照按钮 */
.shutter-button.data-v-7b8d50ad {
  width: 120rpx;
  height: 120rpx;
  border-radius: 50%;
  display: flex;
  justify-content: center;
  align-items: center;
  background: rgba(255, 255, 255, 0.2);
}
.shutter-circle.data-v-7b8d50ad {
  width: 80rpx;
  height: 80rpx;
  border-radius: 50%;
  background: #ffffff;
  border: 4rpx solid rgba(255, 255, 255, 0.6);
  transition: all 0.1s ease;
}
.shutter-button:active .shutter-circle.data-v-7b8d50ad {
  transform: scale(0.95);
  opacity: 0.8;
}

/* 切换摄像头按钮 */
.switch-camera.data-v-7b8d50ad {
  width: 80rpx;
  height: 80rpx;
  display: flex;
  justify-content: center;
  align-items: center;
  background: rgba(255, 255, 255, 0.15);
  border-radius: 50%;
}
.switch-icon.data-v-7b8d50ad {
  width: 40rpx;
  height: 40rpx;
  filter: brightness(200%);
}